This is an interactive dictionary ehich uses json as data source. It takes input word whose meaning user wants to see.
The program also uses difflib libraray to provide close matches in case user gives wrong input.
